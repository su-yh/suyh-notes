


sudo vim /etc/netplan/50-cloud-init.yaml

-- 先删除掉原来的的信息，然后贴入如下内容，根据实际情况修改
network:
  ethernets:
    ens18:  # 你的网卡名（ifconfig 中显示的 ens18）
      dhcp4: no  # 关闭 DHCP
      addresses: [192.168.2.103/24]  # 静态 IP + 子网掩码（/24 对应 255.255.255.0）
      gateway4: 192.168.2.1  # 网关（通常是路由器 IP，根据你的实际网络修改，比如 192.168.2.1）
      nameservers:
        addresses: [8.8.8.8, 114.114.114.114]  # DNS 服务器（谷歌+国内通用）
  version: 2

